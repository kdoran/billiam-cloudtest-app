# URL of the agent-worker's `wrangler dev` (or deployed) endpoint.
AGENT_WORKER_URL=http://localhost:8787/

# The GitHub URL of *this* app repo - the agent clones this inside the
# sandbox, edits it, and boots a preview from the edited copy.
APP_REPO_URL=https://github.com/<you>/<repo>.git
